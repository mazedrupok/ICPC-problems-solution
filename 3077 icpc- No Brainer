#include <stdio.h>
int main ()
{
   int cas, x, y;
   //freopen ("in.txt", "r", stdin);
   scanf (" %d", &cas);
   while (cas--) {
      scanf (" %d %d", &x, &y);
      if (x >= y) printf ("MMM BRAINS\n");
      else printf ("NO BRAINS\n");
   }
   return 0;
}
